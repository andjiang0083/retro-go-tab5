# 参与共建

[English](CONTRIBUTING.md) · **中文**

感谢考虑参与。这是一个由"手上有这块硬件的人"维护的单板移植项目，所以最有价值的贡献是**能在真机 Tab5 上验证的**那些。

## 可以帮什么

- **从 [ROADMAP.md](ROADMAP.md) 里挑一项**，先开个 issue 说明你在做，避免和别人撞车。
- **修 bug** —— 请带上下面说的证据。
- **改进文档** —— 实机截图、Windows/Linux 的构建走查、翻译。
- **移植另一个机种**（NES、SNES、Genesis……）—— retro-go 源码里都有，需要一次目标接线。

## 项目约定（这些来自真实故障，不是口味）

1. **一次刷机只改一个可见的东西。** 刷进去、看屏幕、再继续。一次刷多个改动，出回归时无法归因。
2. **不用串口做调试循环。** 打开 USB-CDC 串口会复位设备（见 BUILDING.md）。用眼睛验证，或从 SD 卡读 `/crash.log`。
3. **任何动显示通路或 PSRAM 带宽的改动，都必须带运行时开关** —— SD 卡上放个文件，启动时读取。
   坏实验必须能**不重刷**就回退。这不是可选项：在这块板上，显示通路的改动会把面板楔死，只有断电才能恢复。
4. **从已知可用的基线出发；宁可小步改动，不要重写。** 一个能验证的 15% 提升，胜过可能是 40% 的重写。

## 构建与测试

见 [BUILDING.md](BUILDING.md)。开 PR 之前：

- [ ] 在**干净树**（不只是增量）里带 `--no-networking` 构建通过。
- [ ] 设备能启动、launcher 可用、GBA 游戏以 59~60 逻辑帧率运行且音频正常。
- [ ] 如果动了显示通路：10 分钟以上无撕裂、无定格。
- [ ] 如果是有风险的改动：已在运行时开关后面，并且你写清了开关怎么用。

## 报 bug

用 issue 表单，并带上：

- **你看到了什么** —— 屏幕行为，如果有的话附上 `FPS:`/`BUSY:` 那行。
- **构建信息** —— 开机打印的 `Build info:` 字符串（目标、版本、SDK、芯片）。
- **`/crash.log`** —— 如果写了的话，从 SD 卡拉出来。**并附上文件日期。**
- **它是怎么恢复的** —— 自己重启了，还是必须断电？这一个细节就能区分软件 panic 和硬件级挂死，两者根因完全不同。

## 提交与 PR 风格

- 推荐用约定式前缀（`feat:`、`fix:`、`docs:`、`refactor:`、`perf:`、`chore:`）。
- **中文或英文都可以**，用哪个能把改动说清楚就用哪个。
- 正文里解释**为什么**，尤其是涉及显示通路或时序的改动。

## 代码风格

跟随周围的代码（上游 retro-go 风格：4 空格缩进、函数的花括号独占一行、框架符号带 `rg_` 前缀）。
本移植特有的改动，请用一句短注释说明它绕开的是哪个板级约束 —— 下一个人需要知道哪些行是上游的、哪些是 Tab5 专属的。
