# 路线图

[English](ROADMAP.md) · **中文**

大致按优先级排序。每项都拆到了新人能上手的粒度。**动手前先在 issue 下留言（或开一个）**，避免撞车。

## 显示通路 —— 每帧推送更少的字节（价值最高）

逻辑速度已经是满速（59~60fps），但真正推到面板上的只有约 15 帧/秒。
最直接的修法 —— 更深的队列 + 三缓冲 —— **已经实测并回退了**（见 README）：它把绘制帧翻到 30/秒，
但会周期性把面板楔死，因为 1280x720@60Hz 的 DPI 扫屏本身就要吃掉约 106MB/s 的 PSRAM 带宽，
没有余量再容纳第二路并发写。

所以安全的方向是每帧**搬更少**的数据，而不是加并发：

- [ ] **只提交核心真正改动过的那些行。** GBA 游戏经常只更新屏幕上一窄条；框架本身已经在跟踪部分帧。
      先量一下典型游戏每帧改了多少行。
- [ ] 在 PR 描述里给出改动前后的绘制帧率实测。
- [ ] 放在运行时开关后面（SD 卡文件），默认保持当前行为。

相关但优先级较低：

- [ ] 研究能否让核心直接渲染成面板需要的字节序，从而完全省掉转置这一步。
- [ ] **带限流地**重新评估 PPA/DMA2D 硬件缩放（只处理小块，绝不整屏突发）。朴素版已知会把 DSI 楔死 —— 见 BUILDING.md。

## 硬件功能

- [ ] **电池电量** —— Tab5 板载 INA226；状态日志现在打印 `BATT:0`。接进 launcher 状态栏和游戏内浮层。
- [ ] **背光控制** —— 在选项菜单里暴露亮度（LEDC 背光已经初始化好了）。

## 模拟器

- [ ] **移植另一个机种** —— NES（nofrendo）是最小的一步；SNES（snes9x）和 Genesis（gwenesis）源码已在树里。
      每个都需要一次目标接线：显示几何、输入映射、音频采样率、存档路径。
- [ ] 把自动跳帧阈值从编译期常量变成可配置项。
- [ ] 按 ROM 记住设置（缩放、跳帧、音量）。

## Launcher / 交互

- [ ] README 里放截图和一段短录像（没有什么比"看见它在跑"更能说明问题）。
- [ ] 封面图与元数据打磨（SD 卡布局里已经有 `romart` 目录）。
- [ ] 收藏 / 最近游玩。
- [ ] 一个能跨重启保持的设置页。

## 构建与 CI

- [ ] **GitHub Actions 构建检查** —— 用官方 `espressif/idf` 容器，至少能证明每个 PR 都能以
      `--target tab5 --no-networking` 编译通过。这是对贡献者体验提升最大的一项。
- [ ] 打 tag 时自动把合并 `.img` 附到 GitHub Release 的工作流。

## 文档

- [ ] Linux 与 Windows 的构建走查。
- [ ] 架构笔记：dynarec 如何映射 GBA 内存、显示任务流水线怎么跑、PSRAM 预算花在哪。
- [ ] README 翻译成更多语言。

## 明确不做

- **任何无线功能。** ESP32-P4 没有 Wi-Fi 也没有蓝牙。这不是移植的限制，是芯片的限制。
- **附带 ROM。** 永不。
